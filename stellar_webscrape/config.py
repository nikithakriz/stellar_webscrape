import logging

Config = {
    'delay': 3,  # Time to wait after opening the URL before scraping content (in seconds).
    'request_delay': 5,  # Time to wait between consecutive requests (in seconds).
    'log_level': logging.INFO,  # Default logging level.
}
